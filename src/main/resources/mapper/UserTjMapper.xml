<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.mapper.UserTjMapper" >
   <select id="getAllByUserSex"   resultType="map">
     SELECT count(1) as zs,
       count(case when user_sex ='1' THEN 0 END)as nan,
       count(case when user_sex='2' then 0 END) as nv
    FROM userentity
    </select>
</mapper>
